# Models

Sequence autoencoder implementations for the HackViz model comparison: LSTM, GRU and Transformer architectures. A hybrid LSTM + attention model may be evaluated as an additional architecture.
