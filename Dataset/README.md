# Datasets

The datasets used for building this project are **cert r-4.2** and **cert r-6.2**.
The model is first trained on **cert r-4.2** data to adapt to malicious behaviour and then transferred to **cert r-6.2** to get trained on more realistic behaviour.

